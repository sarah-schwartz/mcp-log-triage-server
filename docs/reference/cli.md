---
title: CLI
description: Command-line entrypoints.
---

## Stdio Server

```bash
python -m mcp_log_triage_server
```

Or via the console script:

```bash
log-triage-mcp
```

These start a stdio MCP server. Use `LOG_TRIAGE_LOG_LEVEL` to adjust verbosity.
